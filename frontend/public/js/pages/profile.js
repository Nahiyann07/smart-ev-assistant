import { api, emptyState, escapeHtml, formatDate } from '../api.js'; import '../motion.js';
const root = document.querySelector('[data-profile]');
try { const profile = await api('/api/users/me/profile'); const initials = profile.name.split(/\s+/).map(part => part[0]).slice(0, 2).join('').toUpperCase(); root.innerHTML = `<article class="profile-identity panel" data-reveal><div class="profile-avatar" aria-hidden="true">${escapeHtml(initials)}</div><p class="eyebrow">Member since ${formatDate(profile.createdAt).split(',')[0]}</p><h2>${escapeHtml(profile.name)}</h2><p class="muted">${escapeHtml(profile.email)}</p></article><section class="stat-grid" data-reveal><div class="stat"><strong class="stat-value">${profile.favouriteCount}</strong><span class="stat-label">Favourites</span></div><div class="stat"><strong class="stat-value">${profile.reviewCount}</strong><span class="stat-label">Reviews</span></div><div class="stat"><strong class="stat-value">${profile.reportCount}</strong><span class="stat-label">Reports</span></div><div class="stat"><strong class="stat-value">USER</strong><span class="stat-label">Account role</span></div></section>`; } catch (error) { root.innerHTML = emptyState('Could not load profile', error.message); }
